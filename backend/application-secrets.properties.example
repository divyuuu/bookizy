# Copy this file to application-secrets.properties and fill in real values.
# application-secrets.properties is gitignored and must never be committed.
#
# Option 1: Put secrets here (recommended for local dev)
# Option 2: Set environment variables: DB_PASSWORD, JWT_SECRET, etc.

DB_URL=jdbc:mysql://localhost:3306/appointment_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
DB_USERNAME=root
DB_PASSWORD=your_database_password_here

JWT_SECRET=your_jwt_secret_at_least_256_bits_here
JWT_EXPIRATION=86400000
